# Functions — Swift

[Docs](https://docs.swift.org/swift-book/documentation/the-swift-programming-language/functions/)

```swift
func add(a: Int, b: Int) -> Int {
    return a + b
}

// Parameters are labeled by default; callers must use the label
let result = add(a: 3, b: 4)
```
