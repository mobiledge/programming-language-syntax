# Structs — JavaScript

```javascript
// JavaScript has no structs; plain objects serve the same role
const point = { x: 3, y: 4 };
console.log(point.x); // 3

// Non-destructive update via spread
const point2 = { ...point, x: 10 };
```
